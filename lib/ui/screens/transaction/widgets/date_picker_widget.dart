import 'package:flutter/material.dart';

// "Hoy, 04 de febrero" + selector

class DatePicker extends StatelessWidget {
  final DateTime selectedDate;
  
  const DatePicker({
    super.key, 
    required this.selectedDate
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}