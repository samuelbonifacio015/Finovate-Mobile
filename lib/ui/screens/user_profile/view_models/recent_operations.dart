
// Operaciones Recientes
// Operaciones de gastos e ingresos del usuario

class RecentOperations extends StatelessWidget {
  final String title = 'Operaciones Recientes';
  final List operations = []; // Aquí van las operaciones reales
  const RecentOperations({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}